# External Message Samples

Run this before changing Clownfish's GitHub comment tone:

```bash
npm run message-samples
```

The shared renderer lives in `scripts/external-messages.mjs` so source-PR repair comments, replacement-PR comments, duplicate closeouts, and post-merge closeouts stay consistent.
